import { AppBar, Box, Toolbar } from '@mui/material'
import React from 'react'

function Header() {
  return (
    <Box sx={{flexGrow: 1}}>
        <AppBar>
            <Toolbar>
            
            </Toolbar>
        </AppBar>
    </Box>

  )
}

export default Header